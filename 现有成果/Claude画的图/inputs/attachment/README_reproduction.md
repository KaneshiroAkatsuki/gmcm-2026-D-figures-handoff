# 复算入口

先复制整个附件目录到独立工作位置，再按README.md配置Python 3.11及requirements.txt中的依赖。

验证当前方案：python run_checks.py。结果只写运行副本的output/reproduced；所有当前源结果保持不变。

固定决策回算与逐值比较命令见README.md；不要把过程输出覆盖到results。重新绘制28图：python 重新绘制全部图片.py。
