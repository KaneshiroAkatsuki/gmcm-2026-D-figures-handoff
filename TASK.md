# 任务交接说明（Kimi 整理，Claude 云端接手）

> 本文件是任务的唯一权威说明。请先完整阅读，再查看 `现有成果/Claude画的图/` 下的已完成内容，然后从未完成的部分继续。
> 本任务最初在 Windows 本地执行，所有数据已复制进本仓库；**仓库内的相对路径替代了原提示词中的本地绝对路径**，对应关系见第 6 节。

## 1. 原始任务（用户提示词原文）

<details>
<summary>点击展开：2026 华为杯 D 题论文《融合版实验论文》插图绘制任务</summary>

你负责为 2026 华为杯 D 题论文《融合版实验论文》绘制新增插图和改进插图。另一个 AI（GPT）正在修改论文主稿和附件，你不能妨碍它：只读取资料，所有输出只写到指定输出文件夹。等 GPT 忙完，由用户把文件夹交给它插入论文。回复一律用简体中文。

【一、先读（只读）】
1. 插图需求（每张图的数据、画法、位置和要替代的表）：提示词_GPT_修复后新增插图_20260925.md（本仓库 需求与审核/ 下）。这份需求原本是写给 GPT 的，现在改由你来画。插入论文、改正文、重新编号、删表并表仍由 GPT 负责，你不做。
2. 审核背景：融合版实验论文_v004_回报表_20260925.md、融合版实验论文_v004_内容审核_20260925.md（第 4 节是图表优化建议），均在本仓库 需求与审核/ 下。
3. 图表写作规范：参考 gmcm-paper-writing 技能的图表规范；画图时按 dataviz 方法设计配色和图形形式。

【二、数据来源（已复制进仓库，见第 6 节路径映射）】
- v004 冻结结果（唯一正式数值）：q1、q1_energy_first、q2、q3、q4、q4_no_copy_sensitivity 六个 JSON。
- 原题数据与 DEM。
- 现有图源与风格文件：figures/fusion/common.py、figures/new/figstyle.py、flowkit.py、figures/src/*.py、data/*.csv、src/（含正式 q1 程序）。
- 独立几何工具（可选参考）：indep_core.py（本仓库 需求与审核/ 下）。
- Q1 余量敏感性的审核参照值：q1_rho.json（本仓库 需求与审核/ 下），只用于对账，不能作为图的数据。

【三、要画的图（规格见需求文件）】
第一批（必做）：F1～F5
- F1：Q1 返航余量敏感性图，两联。
- F2：连续通信证明原理示意图。
- F3：Q2 运输路线图。
- F4：电池与能源组件使用日历。
- F5：Q4 缺口形成机制图。

第二批：F6～F10
- F6：直连遮挡与中继布设地图。
- F7：单架次时间构成示意图。
- F8：Q4 不可拆单元关系图。
- F9：Q4 缺口件数分布直方图。
- F10：Q2 与 Q3 逐箱送达对比图。

第三批：现有图的改进版
- 图24：分区图放大，组色统一。
- 图20：加大字号，删去重复子图。
- 图2、图18：加地形底图。
- 图6：画四条充电曲线。
- 图22：改用断轴或对数轴。
- 图25 与图26：合并为一幅。
- 图6、13、19、26：清理图内标题。
改进版在原绘图程序的副本上修改，输出到输出文件夹，不动原件。

F1 的数据来源：调用附件里的正式 q1 程序（src/q1_engine.py 和 dcore.py，先看清楚如何传入余量参数 ρ），对 ρ=0.20～0.40（步长 0.01）重算两件事：
- 45 个机型—服务区组合的安全载荷；
- 两种目标顺序下的最少架次和能耗，以及最少架次的全部跳变点。
算完与 q1_rho.json 对账，差异要写进记录。已知参照点：ρ≤0.230892 时最少为 18 架次，之后依次在 0.270497、0.336257、0.338923、0.343743、0.343904、0.347777 处增加，超过 0.352676 时 S008 无法单点往返。F1 的数值最终还要与 GPT 按 M2 的重算结果对账，记录中标注"待与 GPT 的 M2 结果核对"。

画之前先确认这些数据是否一致（都要从 JSON 中重新读取核对，不要照抄）：
- F4 中 A-BAT-06 使 Q2-022 等待 108.936 s；
- F10 的加权平均送达：Q2 为 2931.434 s，Q3 为 3456.307 s；
- F9 的直方图数据取自 q4.json 的 enumeration_statistics；
- F2 使用的 Q3-006 第 3 事件、R-004 区间，时段 2771.26～2856.23 s，最小裕量 0.022010 dB。

【四、硬约束】
- 所有图都用 Python（matplotlib）代码从数据绘制。不生成 AI 图像，不使用截图。
- 每张图一个子文件夹，例如 F01_余量敏感性/，内含：绘图程序 .py；同名 CSV（图中所用的全部数值）；PNG（300 dpi）和 SVG；meta.json（源文件相对路径与 SHA-256、图的尺寸、最小字号、关键数值）。
- 版式：字体宋体加 Times New Roman；通栏宽 15.5 cm，半栏宽 7.5 cm；印刷尺寸下最小字号 10 pt；图内不放标题；配色与现有图一致并保证色盲可辨；不用无意义的 3D、渐变或双纵轴（F1 的副轴例外）。
- 符号标签集中写在一个 labels.json（或 labels.py）中，便于全文记号统一修改后重绘。
- 程序注释只写功能说明，不写任何 AI 或工具声明。输出文件中不能出现本机绝对路径、用户名或身份信息，一律用相对路径。
- 不修改主稿、统一审查包、冻结结果；不启动 Word，不生成 PDF；不使用浏览器自动化，不上传、不外发。
- 篇幅背景：全文上限 85 页（建议 80～83 页），目标图表比约 3:1。新图要信息清楚，但不要为了好看而做得过大。

【五、交付与自检】
- 输出文件夹根目录放：一键重绘全部.py、requirements.txt、图清单.md、给GPT的插入说明.md。
- 自检：另建副本运行一键重绘，PNG 应逐字节一致；图中每个数值都与 JSON 或正式程序输出逐项核对；匿名扫描：PNG 文本块、SVG 元数据、脚本注释中没有 AI/工具署名、绝对路径或身份信息。
- 最后在文件夹中放 交付记录.md：完成哪些图、自检结果、未完成项及原因、F1 对账结果。
</details>

## 2. 当前进度（截至 2026-09-25 10:29，本地会话因额度中断）

输出根目录：`现有成果/Claude画的图/`（所有产出、数据副本、公共库都在里面，自包含）。

| 图号 | 状态 | 说明 |
|---|---|---|
| F1 余量敏感性 | **进行中，卡住** | 扫描程序已写（q1_rho_scan.py），跑出部分结果 q1_rho_scan_part.json；在 ρ=0.3 的 energy_sorties 计算处崩溃：`RuntimeError: 多次超时`（多进程超时，见 F01_余量敏感性/scan.log 末尾）。**需要先修这个超时问题（可调大超时或改串行重跑），再完成扫描、对账、绘图** |
| F2 连续通信证明原理 | ✅ 完成 | py + csv + png + svg + meta.json 齐全 |
| F3 Q2 运输路线 | ✅ 完成 | 同上 |
| F4 电池组件日历 | ✅ 完成 | 同上 |
| F5 Q4 缺口形成机制 | ✅ 完成 | 同上 |
| F6～F10 | 未开始 | 第二批 |
| 第三批改进版（图24/20/2/18/6/22/25/26/13/19） | 未开始 | 在 inputs/attachment 内图源副本上改，不动原件 |
| 图清单.md / 给GPT的插入说明.md / 交付记录.md | 未写 | 交付要求见第 1 节【五】 |

已有公共库：`现有成果/Claude画的图/lib/figkit.py`、`lib/labels.json`（符号标签集中文件，沿用）；根目录已有 `一键重绘全部.py`、`requirements.txt`、`自检.py`。

## 3. 待办事项（按优先级）

1. 修 F1 扫描的超时崩溃（可先读 scan.log 与 q1_rho_scan.py，必要时把多进程改串行或增大超时；云端 CPU 环境不同，注意可复现性）；
2. 完成 F1 扫描 → 与 需求与审核/q1_rho.json 对账（记录差异，标注"待与 GPT 的 M2 结果核对"）→ 画 F1 两联图；
3. 补齐 F1 的 csv/png/svg/meta.json；
4. 第二批 F6～F10（数据核对点见第 1 节，全部从 JSON 重读）；
5. 第三批现有图改进版；
6. 写 图清单.md、给GPT的插入说明.md、交付记录.md；
7. 跑 自检.py 与一键重绘逐字节一致性检查，确认匿名扫描通过。

## 4. 约束与验收标准

- 沿用第 1 节【四】全部硬约束：纯 matplotlib、300dpi PNG+SVG+CSV+meta.json、宋体/Times、通栏 15.5cm/半栏 7.5cm、最小字号 10pt、图内无标题、色盲可辨、无 3D/渐变/双纵轴（F1 副轴例外）；
- 输出里**不得出现本地绝对路径、用户名、AI 或工具署名**（云端沙盒路径尤其注意，meta.json 用仓库内相对路径）；
- 不要改动 `现有成果/Claude画的图/inputs/` 下的数据副本（它们是冻结数据的只读镜像）；
- 已完成且符合要求的 F2～F5 不要推翻重画；如发现数值错误，在 PR 描述中说明后再改；
- 完成后提交 Pull Request，描述里逐项对照本节与第 1 节【五】的自检要求。

## 5. 交接规则

- 只在本仓库内工作，所有输出写到 `现有成果/Claude画的图/` 对应子文件夹；
- 论文主稿修改、插图插入、重新编号由 GPT 负责，**不要做**；
- 回复与文档一律用简体中文；
- 有疑问不要编造数据：能从 JSON 算的就算，算不了的在交付记录里写明原因。

## 6. 路径映射（原本地路径 → 仓库内路径）

| 原提示词中的本地路径 | 仓库内路径 |
|---|---|
| C:\Users\ZhuanZ\Desktop\Claude画的图 | 现有成果/Claude画的图 |
| 最新求解24版\01_最终交付\results（六个冻结 JSON） | 现有成果/Claude画的图/inputs/results |
| 工程文件\...\01_原始资料\D题\数据 | 现有成果/Claude画的图/inputs/raw |
| 融合版实验论文_统一审查包\附件 | 现有成果/Claude画的图/inputs/attachment |
| 审核参照 q1_rho.json / indep_core.py | 需求与审核/ |
| 插图需求与审核背景三个 md | 需求与审核/ |
