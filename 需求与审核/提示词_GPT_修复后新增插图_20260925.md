前提：回报表中的修改已经完成并通过自检（见《提示词_GPT_按回报表修改_20260925.md》）。在此基础上，为《融合版实验论文》新增和改进插图，使全文“图多表少”、更易读。每张图都必须可以由数据和程序重现。

## 硬约束

- 所有图用 Python（matplotlib）或 SVG 代码从数据绘制。**禁止使用 AI 生成的图像**，也禁止使用无源代码的截图。
- 每张图都要提供：绘图程序、同名 CSV、PNG（300 dpi）、SVG，以及记录源 JSON SHA-256 的 meta 文件。风格和目录结构与现有 figures/fusion 保持一致。
- 数据只用 v004 冻结结果、原题数据和 DEM。Q1 余量敏感性数据只能用 M2 中正式程序重算的结果，不得使用审核方的参照值。
- 论文和附件不得出现任何 AI 声明。只用 WPS，不启动 Word，不生成 PDF。
- 统一版式：
  - 字体用宋体加 Times New Roman；
  - 通栏图宽 15.5 cm，半栏图宽 7.5 cm；
  - 印刷尺寸下最小字号 10 pt；
  - 图内不放标题，说明文字写进图题；
  - 配色与现有图一致，并保证色盲可辨；
  - 图中记号与正文完全相同（如果 S4 改过记号，按改后的写）。
- 改主稿前先备份，编辑前后都记录 SHA-256。

## 新增插图（按优先级）

**第一档：必做**

**F1　Q1 返航余量敏感性图（放 6.4，两联）**
- 数据：M2 中正式程序重算的安全载荷结果（ρ 取 0.20–0.40，步长 0.01）；两种目标顺序在各 ρ 下的最少架次和能耗；最少架次的全部跳变点。
- 左图：画出 B-S008，以及 C 型在 S002、S003、S004、S008、S012 的安全载荷随 ρ 的曲线；横线标出机型载重上限；标出 A 型开始受电量限制的 ρ。
- 右图：最少架次随 ρ 的阶梯线，副轴画两种目标顺序的能耗，标出 ρ=0.230892 这一点，并用阴影标出单点往返无法送完全部货箱的区间。
- 正文补 2～3 句结论，与 M1 的证明对应。

**F2　连续通信证明原理示意图（放 8.5.2）**
- 取一个真实区间作示例，建议用全局裕量最小的区间：Q3-006 第 3 事件，R-004 保障，时段 2771.26–2856.23 s。位置数据用 q3.json 中该事件的端点，地形用 DEM 局部窗口。
- 主图：中继点 B 与运动段 A₀A₁ 构成的扫掠三角形，画在 DEM 像元格网的平面投影上。用三种标记分别画出三类候选点：三角形顶点、落在三角形内的格角、三角形边与格线的交点。
- 辅图：沿视线方向的剖面，画出视线高度与像元高程。
- 图题写明：仿射函数在凸多边形上的最小值取在顶点，因此逐像元的检验是精确的，不是抽样。
- 正文补对应一句（与 S3 一致）。

**F3　Q2 运输路线图（放 7.4）**
- 数据：q2.json 各架次的访问顺序与机型。
- 画法：与图18 同一风格；24 架次按机型着色，6 条多点路线加粗或高亮，线宽随同一航段的执行次数增加。
- 标注 O01 和 15 个服务区。

**F4　电池与能源组件使用日历（Q2、Q3 各一联，可替代附录表22 和图14）**
- 数据：q2.json、q3.json 中各任务的开始时刻、返航时刻和 SOC，按式(15) 计算充满时刻；中继任务的开始、返航、实体再可用、组件充满时刻。
- 画法：每块电池或每个组件占一行；飞行占用画实心条，充电尾段画斜线条，条上标任务号。
- 标注 Q2 中 A-BAT-06 使 Q2-022 等待 108.936 s 的位置。
- 删除表22 和图14 后，正文相应改为引用本图。

**F5　Q4 缺口形成机制图（放 9.4.5）**
- 数据：q3.json 的任务时刻，按式(16)、(38) 构造占用区间；分组取 q4.json 两组资源优先方案（G1 为除 S011 外的全部服务区，G2 为 S011）。
- 画法：两联，分别画 B 型机和 B 型电池的并发数随时间变化的阶梯线。每联三条线：不分区、G1、G2。叠加库存线（B 机 2 架、B 电池 4 块），标出两组各自峰值出现的时刻。
- 目的：直观说明“各组峰值出现在不同时刻，相加后超过库存”。

**第二档：建议做**

**F6　直连遮挡与中继布设地图（可与图2 或图18 合并）**
- 底图：DEM 山体阴影。
- 覆盖层：对 DEM 格点（可以每 3×3 个像元取一点）计算“机体位于地面以上 30 m 时”能否与 G01 直连（按双向门限 122 dB，遮挡加 10 dB）。在图题中写明这一高度假设，并注明本图只是示意，不代替连续证明。
- 叠加：以 G01 为圆心、半径 12.511 km 和 3.956 km 的两个距离圈；5 个中继点及其到 G01 的回传连线；15 个服务区。

**F7　单架次时间构成示意图（放 5.4）**
- 运输部分：用表5 中 Q1-001 的实际数值，画一条时间轴（准备 300 s → 装载 210 s → 爬升、巡航、下降 → 交接 432 s → 返航），下面分两行画无人机占用和电池占用，电池行多出 1644.689 s 的充电尾段。
- 中继部分：用 R-002 画同类图，时间轴为准备 → 去程 → 建链 → 服务 → 返航 → 300 s 周转 → 充电，分实体和组件两行。

**F8　Q4 不可拆单元关系图（放 9.2.1 或 9.5.1）**
- 15 个服务区按地理坐标作为节点。
- 同一架次访问的服务区之间连实线（S001—S005）；共用中继的服务区之间连虚线（按全部 25 对保障关系，含 3 对备用）。
- 分两联：主口径得到 14 个单元，禁止复制口径得到 4 个单元，按单元着色。

**F9　Q4 缺口件数分布直方图（替代表14）**
- 数据：q4.json 中 enumeration_statistics 的 shortage_units_histogram，K=2 和 K=3 各一联。
- 纵轴用对数刻度，高亮最小缺口（2 件 3 个分区、4 件 4 个分区）。
- 图题写明枚举总数 8191、788970，以及禁止复制口径的 7、6 个分区。

**F10　Q2 与 Q3 逐箱送达对比图（配合 S1，附录表20、21 可压缩）**
- 80 箱按服务区排列，每箱画 Q2、Q3 两个送达点，并标出期望送达时刻与硬截止。
- 画两条加权平均线：Q2 2931.434 s，Q3 3456.307 s。

**第三档：可选**
- 在图12 中把能耗拆成“水平能耗”与“爬升能耗”两段；
- 三维地形透视图叠加航线和中继点（信息密度低，放最后考虑）。

## 现有图的改进

| 图 | 改法 |
|---|---|
| 图24 分区图 | 放大，改为 1×3 或 2×2 布局；用凸包或底色区分各组；各子图中同一服务区颜色一致；标出 {S001,S005} 单元和中继副本 |
| 图20 通信状态图 | 字号至少 10 pt；按开始时刻排序，必要时拆两栏；删去与图19 重复的“中继服务窗”子图 |
| 图2、图18 | 加 DEM 山体阴影或等高线；拉开 P4、P5 的标签；按直连或中继给航段着色（若做 F6 可合并） |
| 图6 充电曲线 | 画 A、B、C 电池和组件共四条曲线（T 分别为 1800、2400、3000、1800 s） |
| 图6、13、19、26 | 删去图内自带的标题和注释，移入图题 |
| 图22 回传裕量 | R-002 的柱太长，改用断轴或对数轴 |
| 图25、图26 | 合并为一幅：需求柱加库存线，并标出缺口 |
| 图7、11、23 流程图 | 缩小尺寸，或改为算法框 |

## 图表比例与篇幅目标

**目标**
- 图与表的比例约 3 : 1。现状是 28 图、22 表（约 1.3 : 1）。
- 目标为 36 图、12～13 表，即 2.8 : 1～3 : 1。
- 全文页数上限放宽到 85 页（用户 2026-09-25 确认），建议控制在 80～83 页，留出余量；正文不少于 50 页；以 WPS 实测为准。

**图：28 张改为 36 张**
- 删去图14（由 F4 替代）。
- 合并图25 与图26。
- 新增 F1～F10。
- F6 如果并入图2 或图18，则图为 35 张。

**表：22 张改为 12～13 张**

| 原表 | 处理方式 |
|---|---|
| 表2（四类物资属性） | 并入图3（在图中标注类别构成）或写成一句话 |
| 表5（Q1-001 逐段计算） | 由 F7 承担，图中标注表5 的关键数值，正文保留一句算例 |
| 表6（三类链路门限） | 并入表4，增加“双向门限 / 可用距离”两行 |
| 表8（Q1 两方案比较） | 改为表7 的表注加正文一句 |
| 表14（枚举规模与缺口） | 由 F9 替代 |
| 表17（核验范围） | 改为一段文字 |
| 附录表19（Q1 能耗优先 19 行） | 改为一句差异说明：S001 两个 C 型架次的箱分配不同，S008 拆为两个 B 型架次，给出能耗 |
| 附录表20、21（逐箱 80 行） | 分别压缩为“架次—站点”表：Q2 30 行，Q3 26 行；列为架次、站点、箱号后缀、交付完成时刻、该站最紧硬时限余量。逐箱时刻仍能在论文中查到，完整逐箱表另在官方结果工作簿中 |
| 附录表22（Q3 资源使用） | 由 F4 替代 |
| 表15 与表16（Q4） | 合并为一张：各方案分组、分类配置、缺口、CV |
| 表18（附件清单） | 可以保留（此时共 13 张表，比例约 2.8 : 1）；若要严格 3 : 1，改为一段文字说明附件目录 |

以下表格必须保留原生表：符号说明、机型与链路参数、Q1 组批方案、Q2 与 Q3 架次安排、中继点位与任务时刻、Q2/Q3 指标对比、Q4 分区配置。它们承载题目要求的精确输出，不得改成图。

**篇幅估算（仅供参考，以 WPS 实测为准）**
- 新增 F1～F10，加图题和解读文字，约 +5.5 页。
- 各项修正新增的文字，约 +1 页。
- 删表和并表约 -5 页，其中附录逐箱表压缩约 -2.4 页。
- 图14、图25、图26 合并或删除，以及流程图缩小，约 -1.5 页。
- 净变化约 -1 页，预计 79 页左右。
- 在 85 页上限内，多出的版面优先用于图的可读性：F6 可以单独成图；新图不必压得过小，印刷尺寸下字号不低于 10 pt。不要用多出的版面增加新表。
- 如果实测超过 83 页，按下列顺序压缩：
  1. 把图12（17.3 cm）、图21（18.0 cm）、图27（16.3 cm）、图8（14.0 cm）的高度压到 10～12 cm；
  2. 图24 改为 1×3 布局，高度从 22.4 cm 降到约 10 cm；
  3. 图7、11、23 三张流程图，每张控制在 8 cm 左右。

  以上约可再省 2 页。
- 任何情况下全文都不得超过 85 页；需要压缩时，优先压缩图的高度，其次合并图，不要删除题目要求输出的表或图。

## 联动更新与自检

- 正文：每张新图在正文中首次引用，并配 1～3 句解读；全文图号和交叉引用重新编号并逐一核对。
- 附件：同步更新 figures（源代码、CSV、PNG、SVG）、图号目录.md、重新绘制全部图片.py；表格删改后重新导出 paper_tables；更新附录 A 表18 和统一审查包的 files_sha256.json。
- 桌面“融合版实验论文”图源包同步更新。
- 在包外新建副本，运行“python 重新绘制全部图片.py”，全部图重绘后与交付的 PNG 逐字节一致；正文嵌入的图片与附件 PNG 逐字节对应。
- 图中的每个数值都要能追溯到 JSON、原题数据或正式程序输出，逐值核对。
- 再次运行 python run_checks.py，确认通过。
- 重新做匿名扫描：PNG 文本块、SVG 元数据、新脚本注释中都没有 AI 或工具署名、本机路径或身份信息。

## 回报

在 06_协作审查 下你自己的目录提交记录，内容包括：

- 新增图和修改图的清单：图号、题注、所在章节、程序路径、数据来源和 SHA-256；
- 被删除或压缩的表；
- 修改前后的 WPS 页数；
- 自检结果；
- 修改后主稿和送审包清单的 SHA-256；
- 没有完成的项目及原因。
